<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:layout_gravity="bottom"
    android:background="@color/colorPrimary"
    android:gravity="end"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/img_main_current_play_music_thumbnail"
        android:layout_width="?attr/actionBarSize"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="center_vertical"
        android:padding="8dp"
        android:src="@drawable/ic_near_me_black_24dp" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="8dp">

        <TextView
            android:id="@+id/txt_main_current_play_music_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="알 수 없는 음악" />

        <TextView
            android:id="@+id/txt_main_current_play_music_artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/txt_main_current_play_music_title"
            android:text="알 수 없는 아티스트" />


        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_marginBottom="8dp"
            android:layout_marginRight="24dp"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/img_main_current_play_music_previous"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_skip_previous_black_24dp" />

            <ImageView
                android:id="@+id/img_main_current_play_music_play_pause"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginLeft="16dp"
                android:src="@drawable/ic_play_circle_outline_black_24dp" />

            <ImageView
                android:id="@+id/img_main_current_play_music_next"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginLeft="16dp"
                android:src="@drawable/ic_skip_next_black_24dp" />

        </LinearLayout>


    </RelativeLayout>

</LinearLayout>